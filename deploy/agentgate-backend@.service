[Unit]
Description=AgentGate Backend %i
After=network.target

[Service]
Type=simple
User=%u
ExecStart=agentgate-backend --name %i
Restart=always
RestartSec=3
EnvironmentFile=%h/.agentgate/backends/%i/.env

[Install]
WantedBy=multi-user.target
